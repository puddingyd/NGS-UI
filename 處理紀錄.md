# 處理紀錄

## 2026-09-23 — CNV/SV 篩選列與卡片文案精簡

- 依使用者指定格式改為單行英文勾選列，各類顯示數量／總數，移除標題、顯示全部／恢復預設及說明列，背景改為半透明；窄畫面維持一行並可橫向捲動。
- 卡片以英文影響類別、臨床相關基因：GENE (coding exon) 等英文位置描述、HPO match 分數 / 100 呈現。分類、排序與標記保留規則不變。
- 同步更新 AGENTS.md、README.md、frontend/VERSION.md（v9.22）；更新既有前端測試驗證新格式與勾選行為，14 項測試、JavaScript 語法與 diff 檢查通過。

## 2026-09-23 — 全部 CNV/SV 影響篩選與分析排序

- 依使用者擴大範圍，DRAGEN／NCKUH、WGS／WES 的 CNV／SV Clinical／Pathogenic 四分頁都加入三類勾選；預設顯示外顯子／剪接影響與位置註解不足，UTR／內含子可展開，提供顯示全部／恢復預設與顯示數量。
- Clinical 只按 HPO／panel 命中基因分類；Pathogenic 按所有基因分類。P／LP 在 Clinical 固定顯示，Pathogenic 依選項篩選；所有頁面的 1／2／C 標記固定顯示。合併事件保留成員證據與標記，不將片段間空隙當成受影響區域。
- 分析排序改為 P／LP、影響類型、獨立 HPO 分數、方向相符 dosage 證據、原始 AnnotSV 分數與 ID；不變更 ACMG、tier membership 或報告選取／排序。修正 split row 先於 full row 時被覆蓋而遺失基因註解的情況。
- 以 CDS 實際重疊長度與剪接距離判斷，Location2=CDS 不直接當 exon；缺欄位與其他 SV type 尚不足以判斷斷點影響時保留 unknown。更新服務並重新載入個案即可套用，無須重跑三級。
- 驗證：172 項後端、18 項前端行為測試通過；涵蓋位置判斷、基因範圍、合併、獨立 HPO、四分頁渲染、空結果、勾選、標記保留、跨 caller／WGS／WES 與既有 rescue／報告回歸。JavaScript 語法與 diff 檢查通過。瀏覽器連不到本機預覽且 Chrome 未提供，未完成實際瀏覽器版面驗證；未部署伺服器。
- 使用者輸入 go 後先讀待處理事項.md，該檔沒有項目，故未清除任何待辦；完成本對話已授權的 CNV/SV 修改。AGENTS.md、README.md、frontend/VERSION.md（v9.21）同步更新。

## 2026-09-22 — DRAGEN CNV rescue

- 依討論定案的 A／B 二元規則，在 DRAGEN worker 的 post-processing 加入原始 CNV＋整合 CNV/SV 比對；僅放寬 cnvLength／cnvQual，不新增候選檢視流程，in-house 不執行。
- 新事件補 AnnotSV，於 08 產生 CNV review／rescue TSV 與版本化追溯 JSON；所有 CNV 載入與報告路徑共用 resolver，保留原有 ID，新事件使用穩定 ID。加入原子發布、回復、重跑清理與失敗檢查。
- 卡片提供原始 Filter／Qual、原始／整合座標、規則與支持 SV；合併卡片彙整所有片段證據。AGENTS.md、README.md、VERSION.md（v9.19）、ANALYSIS_FLOW.md 同步更新。
- 檔案唯讀驗證：VAL-29 使用完整附件重現 A=222、B=11；VAL-25／VAL-28 使用附件整合檔及前文提供的原始單筆紀錄，分別命中 B／A。未提交患者 VCF；測試使用合成資料。桌面未執行正式 reference 的 AnnotSV／伺服器整條 pipeline。
- 驗證：144 項後端測試、7 項前端行為測試通過；涵蓋規則邊界、追溯資訊、補註解失敗、去重、重跑清理、發布回復、in-house 排除與批次進度。JavaScript 語法與 diff 空白檢查通過。

## 2026-09-14 — Documents 連續預覽與 ClinVar 縮寫配色

- 主畫面與 phenotype 工具共用的 Documents 預覽新增左右檔案按鈕、←／→ 快捷鍵與檔案序號，略過不可預覽的 PDF；TIFF 頁內翻頁獨立保留，切換新檔案回第一頁。
- 預覽取消及快速切換使用中止請求／序號檢查，避免過期回應覆蓋新圖或在關閉後重新顯示；清單也避免跨 MRN 的過期回應。
- ClinVar 的 P／LP／VUS／LB／B 縮寫相容空白、大小寫與計數前底線，保留各分項筆數、星等和 hover 完整名稱；每個分項套自己的紅／黃／綠底色，P 與 B 共存時各自呈現。套用於 SNV/Indel 與 Mito 共用卡片，不改 tier、ACMG 或 DOCX 判讀資料。
- 驗證：10 項 Node 行為測試、39 項既有前端檢查、JavaScript 語法檢查通過；使用合成文件在 Chrome 驗證圖片／TIFF／鍵盤／首尾停用、窄螢幕版面，以及快速切換與載入中關閉預覽。已同步更新 AGENTS.md、README.md、frontend/VERSION.md（v9.13）。

## 2026-06-15

- 新增右上角「二級分析」入口，位置在「三級分析」左邊；登入後可開啟 FASTQ samplesheet 工具。
- 後端新增 `/api/secondary/fastqs`、`/api/secondary/index/refresh`、`/api/secondary/samplesheet`，可掃描 WES/WGS FASTQ、快取索引、建立 `samplesheet.csv` 並回傳 DGX2 tmux 執行指令。
- WES FASTQ 搜尋支援 NextSeq2000 與 Reanalysis；WGS 一律使用 lane FASTQ，不使用 merged FASTQ，並在 samplesheet 加 `lane` 欄。
- 建立二級分析 samplesheet 時，FASTQ 路徑會轉成 DGX2 可見路徑，移除最前面的 `/home`（例如 `/home/datalake_Raw/...` → `/datalake_Raw/...`）。
- 前端支援 WES/WGS 兩個搜尋區、批次加入、「加入同批全部樣本」、自訂 batch name、複製 tmux 指令，以及 tmux attach / detach 提示。
- Reanalysis FASTQ 建立 samplesheet 時改為直接指向原始 `*.R1/R2.clean.fastq.gz`，不複製、不建立 renamed symlink；修改 Sample ID 只影響 samplesheet 的 `sample` 欄。
- 二級分析 samplesheet 改為先寫到 `nextflow_samplesheet_staging/<batch>/samplesheet.csv`；產生的 DGX2 tmux 指令會由 DGX2 runner 建立 `OUT_DIR`，再把 staged samplesheet 複製到 `OUT_DIR/samplesheet.csv`，避免 NGS-UI 建 output dir 造成 owner/permission 問題。
- 二級分析 DGX2 script 加入 `finish` trap：Nextflow 成功或失敗都會複製 `${LAUNCH_DIR}/.nextflow.log` 到 `${OUT_DIR}/nextflow.log`，印出 DONE/FAILED 與 exit status，最後 `exec bash -i` 保留 tmux pane，避免跑完直接關閉。
- 修正二級分析 UI：WES FASTQ / WGS FASTQ 改為上下排列；預設 Reanalysis root 補上 `/home/datalake_Raw/Other/Reanalysis`；複製按鈕在 Clipboard API 被內網 HTTP/權限擋住時會 fallback 到 textarea copy；若 HTML 快取漏掉按鈕，前端會動態把「二級分析」插到「三級分析」左邊。
- 已同步更新 `AGENTS.md`、`README.md` 與 `frontend/VERSION.md`。

## 2026-06-14

- ACMG SF / Proactive / Carrier screening 改為 secondary finding selection：三區分別使用 custom panel `ACMG_SF_v3.3`、`proactive`、`carrier_mackenzie_1300+`，從 `snv_indel.review.tsv` 只列 SNV/Indel ClinVar P/LP 或卡片最終 ACMG P/LP，並套用 VAF ≥ 0.2、zygosity 非 ref。
- ClinVar P/LP 預設 ✓；ACMG P/LP 但非 ClinVar P/LP 預設留在分析區。手動勾選與取消分別保存到 `secondary_findings.{section}.selected/dismissed`，避免取消後下次載入又被預設勾回。
- 後端 secondary finding 不再掃完整 raw TSV 或用 carrier panel gene index 補點，避免 BED 外、低 VAF 或 zygosity=ref 的位點進入 secondary 區。
- 調整 secondary findings：分析區與報告區預設展開；分析區保留全部候選，報告區只顯示 ✓；✓ 改為紫色，SNV comment / ACMG score / criteria 於兩邊同步。
- 將 secondary findings 改成 staged loading：核心 sample payload 先顯示主要 SNV/Indel，ACMG SF / Proactive / Carrier 透過 `/secondary-snv` 背景補載，來源同主畫面 compact review TSV。
- WES SNV/Indel read depth < 10 改為後端直接濾掉：`snv_indel.review.tsv` 產生階段、主畫面載入、gene search 與 reviewer-marked 補點都套同一個 gate；`run_stopgaps.sh` 會把 sample `seq_type` 傳給 review TSV builder，WGS 不套用此 hard floor。
- 已同步更新 `AGENTS.md`、`README.md` 與 `frontend/VERSION.md`。

## 2026-06-11

- 三級分析 Nextflow 進度改成 weighted progress：`queued` 事件只寫 timing log，不再推進 UI 百分比；實際 `start/done` 才依 process 權重更新。
- 前處理、mito、STR、prepare CNV/SV 等快速步驟只佔少量進度，`ANNOTSV_SV`、VEP、Pangolin、parse CSQ、ACMG/PGx 等耗時步驟佔主要權重。
- 前端 copy / stop-gaps 起點從 87% 調為 82%，保留更多百分比給 GeneBe、Extra VEP、AnnotSV、review TSV 與 gene index 預建。

## 2026-06-10

- In-house 三級分析預設 Sample ID 改為加上 `-nckuh` 後綴，並讓 pipeline output 端也產生對應的 `-nckuh` 目錄；`-inhouse` 僅保留為舊資料 alias。
- 後端 job API 與 worker 也會強制 normalize 新 job 的 sample ID：DRAGEN 補 `-dragen`、in-house 補 `-nckuh`，避免舊前端快取或手動輸入造成 pipeline output 仍落在無 suffix 目錄。
- 三級分析 Nextflow 進度改為單調遞增；batch job 中較早 process 若晚於後面 process 更新，不再把 `nextflow_progress_pct` 覆蓋成較小百分比。
- SNV/Indel `MANE_ALL` 解析改為容忍 CSV doubled quotes / smart quotes，並優先用有 HGVS.c/p、非 MODIFIER 的 MANE_SELECT `NM_` transcript 顯示同基因搜尋與 More 表格。
- 統一 `Disease-associated` / `In panel` filter count 字體，並讓 DRAGEN caller badge 使用和 DV/HC 一致的暖色 badge 樣式。

## 2026-06-09

- 三級分析 pipeline output reuse 改為檢查 SNV、Mito、CNV、SV，以及 PGx checkbox 有勾時的 PGx/PharmCAT 輸出；缺任一輸出會重新進 Nextflow `-resume` 補跑，不再只因既有 acmg.tsv 就跳過。
- `/home/pipeline/tertiary_output/` 新邏輯優先使用 UI sample ID（例如 `-dragen` / `-inhouse`）分流；舊 source-ID-only output 會作為 legacy fallback，並新增 `scripts/repair_pipeline_output_suffixes.py` 供已跑完 case 複製成 suffixed 目錄。
- `scripts/repair_pipeline_output_suffixes.py` 補強為也會直接掃 pipeline output，預設將 `VAL-數字` 無 suffix 目錄視為 legacy DRAGEN，複製成 `VAL-數字-dragen`，處理沒有 NGS-UI sidecar 的舊資料。
- 三級分析 log 改成行尾 `[YYYY-MM-DD HH:MM:SS]` timestamp，Nextflow process 另記錄 queued/start/done、done/total 與 elapsed，進度條可依 batch 內 process 完成比例推進。
- SNV 同基因搜尋與 MANE 詳細表加強 RefSeq `NM_` transcript 顯示，後端也更寬容解析不同 pipeline 版本的 `MANE_ALL` 欄位。
- 已同步更新 `AGENTS.md`、`README.md` 與 `frontend/VERSION.md`。

## 2026-06-01

- 個案核心資料載入與新個案登錄期間新增「個案載入中」遮罩；遮罩不可用背景點擊或 ESC 誤關閉，避免使用者重複操作。
- 主畫面 Patient phenotype 的 Gene panels 預設展開 `Other panel`，並把工具連結改為較符合頁面風格的深綠色。
- 「載入新個案」modal 的 phenotype panel 選擇補上 `WES-I / WES-II / WGS / Other panel` tabs，預設展開 `Other panel`；固定 panel chip 與 Other panel 搜尋下拉會顯示基因數量。
- 三級分析 VCF 清單的檔案建立時間確認為後端檔案 `mtime` Unix timestamp，前端改為固定以 `Asia/Taipei`（UTC+8）顯示。
- Patient phenotype 的「輸入臨床表徵 (HPO/panel)」提示連結改為粗體黑色加底線。
- 平台剛開啟讀取樣本索引時也顯示共用的「資料載入中」遮罩；遮罩文案改為通用名稱，讓索引讀取與個案載入都適用。
- DOCX 診斷報告調整為先列第一類、第二類與家族檢測建議，再集中列參考資料；SNV/Indel 補上 TSV `RS_ID`，SNV/Indel 與 Mito 核苷酸欄每行最多 13 字元且蛋白質括號固定換行。
- DOCX 遺傳模式更新 `XL / XLD / XLR` 中文，`Likely pathogenic` 改為「疑似致病性」；§五.4 gene list 移除左側縮排與 HPO ID。
- 三級分析清單改為合併 pipeline output 與 job state，讓失敗且無 output 的 sample 仍可查看 log；刪除 sample 時同步刪除相關 job log 目錄。
- 三級分析 Extra VEP tooltip 僅顯示補 SpliceAI 註解，工具列按鈕固定高度且禁止換行；修正病歷號區塊的 CSS selector，使其確實向上對齊。
- DOCX 的 SNV/Indel `RS ID` 欄改為預留尾端空格，避免內容與後方結構欄黏在一起；§五.4 grouped gene list 在各 HPO/panel 區塊之間加入空行。
- DOCX 匯出補上 SNV 無 OMIM disease 文字時的空值防護，避免整份報告因空字串中斷。
